bin/
coverage.out
*.out
*.log
*.test
.env
.env.*
!.env.example

# `go build ./cmd/<name>` without -o writes command binaries at the repository root.
/apierrcodes
/danshi-migrate
/danshi-server
/openapi

# AI 协作过程记录，不进公开仓库
.codex/

# 测试环境部署资产：含服务器 IP 与内网拓扑，不进公开仓库
deploy/test/
